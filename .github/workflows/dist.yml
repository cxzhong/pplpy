name: Distributions

on:
  push:
    branches: [ "master" ]
    tags: [ "*" ]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

  sdist:
    name: Build sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgmp-dev libmpfr-dev libmpc-dev

      - name: Build sdist
        run: |
          pip install build
          python -m build --sdist

      - uses: actions/upload-artifact@v6
        with:
          name: dist-sdist
          path: dist/*.tar.gz

  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: sdist
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - macos-15-intel
          - ubuntu-24.04-arm
    env:
      CIBW_BUILD: "cp312-* cp313-* cp314-* cp314t-*"
      CIBW_ENABLE: "cpython-freethreading"
      CIBW_BEFORE_ALL_LINUX: >-
        (command -v yum > /dev/null && yum install -y epel-release && yum install -y gmp-devel glpk-devel || true) &&
        (command -v apk > /dev/null && apk add --no-cache gmp-dev glpk-dev m4 g++ || true)
      CIBW_REPAIR_WHEEL_COMMAND_LINUX: "python -c \"import zipfile,os,subprocess,tempfile,pathlib,shutil;whl='{wheel}';dest='{dest_dir}';tmp=tempfile.mkdtemp();zipfile.ZipFile(whl).extractall(tmp);[subprocess.run(['strip','--strip-debug',str(p)]) for p in pathlib.Path(tmp).rglob('*.so')];out=os.path.join(tmp,os.path.basename(whl));shutil.make_archive(out.removesuffix('.whl'),'zip',tmp);os.rename(out.removesuffix('.whl')+'.zip',out);subprocess.run(['auditwheel','repair','-w',dest,out],check=True)\""
      CIBW_REPAIR_WHEEL_COMMAND_MACOS: "python -c \"import zipfile,os,subprocess,tempfile,pathlib,shutil;whl='{wheel}';dest='{dest_dir}';tmp=tempfile.mkdtemp();zipfile.ZipFile(whl).extractall(tmp);[subprocess.run(['strip','-x',str(p)]) for p in list(pathlib.Path(tmp).rglob('*.so'))+list(pathlib.Path(tmp).rglob('*.dylib'))];out=os.path.join(tmp,os.path.basename(whl));shutil.make_archive(out.removesuffix('.whl'),'zip',tmp);os.rename(out.removesuffix('.whl')+'.zip',out);subprocess.run(['delocate-wheel','--require-archs','{delocate_archs}','-w',dest,out],check=True)\""
      CIBW_BEFORE_ALL_MACOS: >-
        brew install gmp glpk &&
        rm -f $(brew --prefix gmp)/lib/libgmp*.dylib $(brew --prefix gmp)/lib/libgmpxx*.dylib &&
        rm -f $(brew --prefix glpk)/lib/libglpk*.dylib
      CIBW_ENVIRONMENT_MACOS: >-
        MACOSX_DEPLOYMENT_TARGET=15.0
        CPATH=$(brew --prefix gmp)/include:$(brew --prefix glpk)/include:$CPATH
        LIBRARY_PATH=$(brew --prefix gmp)/lib:$(brew --prefix glpk)/lib:$LIBRARY_PATH
      CIBW_BUILD_FRONTEND: build
      CIBW_TEST_COMMAND: >-
        python {project}/tests/runtests.py
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Build wheels
        uses: pypa/cibuildwheel@v3.3.1
        with:
          output-dir: wheelhouse

      - uses: actions/upload-artifact@v6
        with:
          name: wheels-${{ matrix.os }}
          path: ./wheelhouse/*.whl

  pypi_publish:
    name: Upload to PyPI
    needs: [sdist, build_wheels]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v7
        with:
          path: dist
          merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          user: __token__
          password: ${{ secrets.SAGEMATH_PYPI_API_TOKEN }}
          packages-dir: dist/
          skip-existing: true
          verbose: true
